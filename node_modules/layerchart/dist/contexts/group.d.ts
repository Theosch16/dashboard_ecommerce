import { ChartGroupState } from '../states/group.svelte.js';
/**
 * Get the chart group provided by an ancestor `<ChartGroup>`, or `undefined` when there is none.
 *
 * Charts prefer an explicit `group` prop and fall back to this.
 */
export declare function getChartGroup(): ChartGroupState | undefined;
export declare function setChartGroup(group: ChartGroupState): ChartGroupState | undefined;
