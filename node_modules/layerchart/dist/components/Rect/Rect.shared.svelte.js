import { untrack } from 'svelte';
import { hasAnyDataProp, resolveDataProp, resolveGeoDataPair } from '../../utils/dataProp.js';
import { resolveCorners, cornersUniform, resolveInsets, } from '../../utils/rect.svelte.js';
import { roundedRectPath, parseDashArray } from '../../utils/path.js';
import { createMotion, createDataMotionMap, parseMotionProp, } from '../../utils/motion.svelte.js';
import { getChartContext } from '../../contexts/chart.js';
import { getMarkData } from '../../contexts/facet.js';
import { getGeoContext } from '../../contexts/geo.js';
const defaultKey = (_, i) => i;
/** Build the standard `markInfo` payload used by every Rect variant. */
export function rectMarkInfo(props, dataMode) {
    if (!dataMode)
        return {};
    return {
        data: props.data,
        x: typeof props.x === 'string' ? props.x : undefined,
        y: typeof props.y === 'string' ? props.y : undefined,
        color: typeof props.fill === 'string'
            ? props.fill
            : typeof props.stroke === 'string'
                ? props.stroke
                : undefined,
    };
}
/**
 * Reactive state shared by every per-layer Rect variant.
 */
export class RectState {
    #getProps = () => ({});
    /**
     * Memoized props. `#getProps()` allocates a fresh object (it spreads `rest`),
     * so calling it once per derived meant ~30 allocations per instance per update.
     */
    #props = $derived(this.#getProps());
    // Contexts
    chartCtx = getChartContext();
    markData = getMarkData();
    geo = getGeoContext();
    // Data mode detection
    hasEdgeProps = $derived(hasAnyDataProp(this.#props.x0, this.#props.y0, this.#props.x1, this.#props.y1));
    dataMode = $derived(hasAnyDataProp(this.#props.x, this.#props.y, this.#props.width, this.#props.height) ||
        this.hasEdgeProps);
    // Data resolution
    #resolvedData = $derived(this.dataMode ? this.markData(this.#props.data) : []);
    resolvedItems = $derived.by(() => {
        if (!this.dataMode)
            return [];
        const props = this.#props;
        const keyFn = props.key ?? defaultKey;
        return this.#resolvedData.map((d, i) => {
            const key = keyFn(d, i);
            const resolved = this.#resolveRect(d);
            const animated = this.#dataMotionMap?.get(key);
            return {
                d,
                key,
                x: animated?.x ?? resolved.x,
                y: animated?.y ?? resolved.y,
                width: animated?.width ?? resolved.width,
                height: animated?.height ?? resolved.height,
            };
        });
    });
    #resolveRect(d) {
        const props = this.#props;
        const resolvedInsets = resolveInsets(props.insets);
        if (this.hasEdgeProps) {
            let rx0, rx1p, ry0, ry1p;
            if (this.geo.projection) {
                [rx0, ry0] = resolveGeoDataPair(props.x0, props.y0, d, this.geo.projection);
                [rx1p, ry1p] = resolveGeoDataPair(props.x1, props.y1, d, this.geo.projection);
            }
            else {
                rx0 = resolveDataProp(props.x0, d, this.chartCtx.xScale, 0);
                rx1p = resolveDataProp(props.x1, d, this.chartCtx.xScale, 0);
                ry0 = resolveDataProp(props.y0, d, this.chartCtx.yScale, 0);
                ry1p = resolveDataProp(props.y1, d, this.chartCtx.yScale, 0);
            }
            const left = Math.min(rx0, rx1p) + resolvedInsets.left;
            const right = Math.max(rx0, rx1p) - resolvedInsets.right;
            const top = Math.min(ry0, ry1p) + resolvedInsets.top;
            const bottom = Math.max(ry0, ry1p) - resolvedInsets.bottom;
            return {
                x: left,
                y: top,
                width: Math.max(0, right - left),
                height: Math.max(0, bottom - top),
            };
        }
        else {
            let resolvedX, resolvedY;
            if (this.geo.projection) {
                [resolvedX, resolvedY] = resolveGeoDataPair(props.x, props.y, d, this.geo.projection);
            }
            else {
                resolvedX = resolveDataProp(props.x, d, this.chartCtx.xScale, 0);
                resolvedY = resolveDataProp(props.y, d, this.chartCtx.yScale, 0);
            }
            return {
                x: resolvedX + resolvedInsets.left,
                y: resolvedY + resolvedInsets.top,
                width: Math.max(0, resolveDataProp(props.width, d, undefined, 0) - resolvedInsets.left - resolvedInsets.right),
                height: Math.max(0, resolveDataProp(props.height, d, undefined, 0) -
                    resolvedInsets.top -
                    resolvedInsets.bottom),
            };
        }
    }
    // Dash array
    dashArrayResolved = $derived(parseDashArray(this.#props.dashArray));
    dashArrayAttr = $derived(this.dashArrayResolved ? this.dashArrayResolved.join(' ') : undefined);
    // Corners
    cornersUniformValue = $derived.by(() => {
        const corners = this.#props.corners;
        if (corners === undefined)
            return undefined;
        if (typeof corners === 'number')
            return corners;
        const resolved = resolveCorners(corners, Infinity, Infinity);
        return cornersUniform(resolved) ? resolved[0] : undefined;
    });
    cornersNonUniform = $derived(this.#props.corners !== undefined && this.cornersUniformValue === undefined);
    // Normalize rx/ry: if only one provided, use for both (SVG behavior)
    rx = $derived(Number(this.#props.rx ?? this.#props.ry ?? this.cornersUniformValue) || 0);
    ry = $derived(Number(this.#props.ry ?? this.#props.rx ?? this.cornersUniformValue) || 0);
    // Pixel-mode motion sources
    #dataMotionMap = null;
    #motionX;
    #motionY;
    #motionWidth;
    #motionHeight;
    get motionX() {
        return this.#motionX.current;
    }
    get motionY() {
        return this.#motionY.current;
    }
    get motionWidth() {
        return this.#motionWidth.current;
    }
    get motionHeight() {
        return this.#motionHeight.current;
    }
    // Resolved per-corner radii (clamped to current bounds)
    resolveCorners(width, height) {
        const corners = this.#props.corners;
        if (corners === undefined)
            return undefined;
        return resolveCorners(corners, width, height);
    }
    roundedRectPath(x, y, width, height) {
        const corners = this.resolveCorners(width, height);
        if (!corners || !this.cornersNonUniform)
            return undefined;
        return roundedRectPath(x, y, width, height, corners);
    }
    borderRadius(width, height) {
        const corners = this.resolveCorners(width, height);
        return corners ? corners.map((c) => `${c}px`).join(' ') : undefined;
    }
    resolvedCorners = $derived.by(() => {
        return this.resolveCorners(this.motionWidth, this.motionHeight);
    });
    borderRadiusStyle = $derived(this.resolvedCorners ? this.resolvedCorners.map((c) => `${c}px`).join(' ') : undefined);
    pixelPathData = $derived.by(() => {
        if (this.resolvedCorners && this.cornersNonUniform) {
            return roundedRectPath(this.motionX, this.motionY, this.motionWidth, this.motionHeight, this.resolvedCorners);
        }
        return undefined;
    });
    // Static (non-data-driven) values for SVG/HTML pixel mode
    staticFill = $derived(typeof this.#props.fill === 'string' ? this.#props.fill : undefined);
    staticFillOpacity = $derived(typeof this.#props.fillOpacity === 'number' ? this.#props.fillOpacity : undefined);
    staticStroke = $derived(typeof this.#props.stroke === 'string' ? this.#props.stroke : undefined);
    staticStrokeOpacity = $derived(typeof this.#props.strokeOpacity === 'number'
        ? this.#props.strokeOpacity
        : undefined);
    staticStrokeWidth = $derived(typeof this.#props.strokeWidth === 'number' ? this.#props.strokeWidth : undefined);
    staticOpacity = $derived(typeof this.#props.opacity === 'number' ? this.#props.opacity : undefined);
    staticClassName = $derived(typeof this.#props.class === 'string' ? this.#props.class : undefined);
    // Match SVG's implicit `stroke-width: 1` default
    staticBorderWidth = $derived.by(() => {
        const props = this.#props;
        if (typeof props.strokeWidth === 'number')
            return `${props.strokeWidth}px`;
        if (typeof props.stroke === 'string')
            return '1px';
        return undefined;
    });
    constructor(getProps) {
        this.#getProps = getProps;
        const initial = getProps();
        const initialX = initial.initialX ?? (typeof initial.x === 'number' ? initial.x : 0);
        const initialY = initial.initialY ?? (typeof initial.y === 'number' ? initial.y : 0);
        const initialWidth = initial.initialWidth ?? (typeof initial.width === 'number' ? initial.width : 0);
        const initialHeight = initial.initialHeight ?? (typeof initial.height === 'number' ? initial.height : 0);
        const motion = initial.motion;
        this.#motionX = createMotion(initialX, () => (typeof this.#props.x === 'number' ? this.#props.x : 0), motion === undefined ? undefined : parseMotionProp(motion, 'x'));
        this.#motionY = createMotion(initialY, () => (typeof this.#props.y === 'number' ? this.#props.y : 0), motion === undefined ? undefined : parseMotionProp(motion, 'y'));
        this.#motionWidth = createMotion(initialWidth, () => (typeof this.#props.width === 'number' ? this.#props.width : 0), motion === undefined ? undefined : parseMotionProp(motion, 'width'));
        this.#motionHeight = createMotion(initialHeight, () => (typeof this.#props.height === 'number' ? this.#props.height : 0), motion === undefined ? undefined : parseMotionProp(motion, 'height'));
        this.#dataMotionMap = createDataMotionMap(motion);
        if (this.#dataMotionMap) {
            const motionMap = this.#dataMotionMap;
            $effect(() => {
                if (!this.dataMode)
                    return;
                const props = this.#props;
                const keyFn = props.key ?? defaultKey;
                const activeKeys = new Set();
                for (let i = 0; i < this.#resolvedData.length; i++) {
                    const d = this.#resolvedData[i];
                    const key = keyFn(d, i);
                    activeKeys.add(key);
                    const resolved = this.#resolveRect(d);
                    untrack(() => motionMap.update(key, resolved));
                }
                untrack(() => motionMap.cleanup(activeKeys));
            });
        }
    }
}
