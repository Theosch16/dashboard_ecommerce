import type { Component } from 'svelte';
import type { BrushState } from '../../states/brush.svelte.js';
export type BrushProps = {
    /**
     * The selection to drive.  Omit it and one is created, which `bind:state` reads back.
     *
     * @bindable
     */
    state?: BrushState;
    /** Which axis the selection spans.  Only used when creating the state. @default 'x' */
    axis?: 'x' | 'y' | 'both';
    /** The brushable region, defaulting to the plot area */
    x?: number;
    y?: number;
    width?: number;
    height?: number;
    /** Size of the resize handles, in pixels @default 8 */
    handleSize?: number;
    /** Called as the selection changes, and once more when the gesture ends */
    onChange?: (detail: {
        state: BrushState;
        phase: 'start' | 'brush' | 'end';
    }) => void;
    classes?: {
        root?: string;
        selection?: string;
        handle?: string;
    };
};
export type BrushBaseProps = BrushProps & {
    Rect: Component<any>;
};
declare const Brush: Component<BrushBaseProps, {}, "state">;
type Brush = ReturnType<typeof Brush>;
export default Brush;
