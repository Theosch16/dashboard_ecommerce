import { curveBumpX, curveBumpY } from 'd3-shape';
export const LINK_FALLBACK_COORDS = { x: 0, y: 0 };
export function isAccessorAccessor(value) {
    return typeof value === 'string' || typeof value === 'function';
}
export { curveBumpX, curveBumpY };
