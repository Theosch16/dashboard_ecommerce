/** Mount/destroy tally, so a test can assert the chart builds its children exactly once. */
export declare const mountCounts: {
    mount: number;
    destroy: number;
};
/**
 * The plot size this child saw while it was being constructed.
 *
 * Marks capture the scales as they are built — a motion baseline, an initial position — so this
 * is what those reads would have returned. It must already be the measured layout, never the
 * placeholder `ChartState` starts from.
 */
export declare const sizeAtConstruction: {
    width: number;
    height: number;
    containerWidth: number;
};
export declare function resetMountCounts(): void;
interface $$__sveltets_2_IsomorphicComponent<Props extends Record<string, any> = any, Events extends Record<string, any> = any, Slots extends Record<string, any> = any, Exports = {}, Bindings = string> {
    new (options: import('svelte').ComponentConstructorOptions<Props>): import('svelte').SvelteComponent<Props, Events, Slots> & {
        $$bindings?: Bindings;
    } & Exports;
    (internal: unknown, props: {
        $$events?: Events;
        $$slots?: Slots;
    }): Exports & {
        $set?: any;
        $on?: any;
    };
    z_$$bindings?: Bindings;
}
declare const MountCounter: $$__sveltets_2_IsomorphicComponent<Record<string, never>, {
    [evt: string]: CustomEvent<any>;
}, {}, {}, string>;
type MountCounter = InstanceType<typeof MountCounter>;
export default MountCounter;
